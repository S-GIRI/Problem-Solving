class Solution{
    public:
    int sameChar(string A, string B)
    {
        // code here 
        int n=A.length(),count=0;
        for(int i=0;i<n;i++){
            if(A[i]==B[i]||(A[i]+32==B[i])||(A[i]-32==B[i])){
                count++;
            }
        }
        return count;
    }
};